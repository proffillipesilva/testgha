import React from 'react'
import PropTypes from 'prop-types'

const HomePage = props => {
  return (
    <div>
        <nav>
            <span>
                 nome <img src={""} />
            </span>
        </nav>
        HomePage
    </div>
  )
}

HomePage.propTypes = {}

export default HomePage